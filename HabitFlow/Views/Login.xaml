<Window x:Class="HabitFlow.Views.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HabitFlow"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800"
         Style="{StaticResource MaterialDesignWindow}"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <!-- левый отступ -->
            <ColumnDefinition Width="1.5*"/>
            <!-- центр - форма -->
            <ColumnDefinition Width="*"/>
            <!-- правый отступ -->
        </Grid.ColumnDefinitions>
        <StackPanel MaxWidth="500" Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" >

            <TextBlock
                
            Padding="0,16"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Style="{StaticResource MaterialDesignBody1TextBlock}"
            Text="Авторизация"/>

            <TextBox
            Style="{StaticResource MaterialDesignOutlinedTextBox}"
                materialDesign:TextFieldAssist.HasLeadingIcon="True"
                materialDesign:TextFieldAssist.LeadingIcon="User"
                materialDesign:HintAssist.HelperText="Такого пользователя не существует"
                materialDesign:HintAssist.Hint="Имя пользователя"
                
 >
            </TextBox>
            <PasswordBox
                materialDesign:TextFieldAssist.LeadingIcon="Password"
                materialDesign:TextFieldAssist.HasLeadingIcon="True"
                Style="{StaticResource MaterialDesignOutlinedRevealPasswordBox}"
                Margin="0, 18, 0, 0"
                materialDesign:HintAssist.HelperText="Неверный пароль"
                materialDesign:HintAssist.Hint="Пароль"
                ></PasswordBox>

            <Button
                FontSize="20"
                Height="50"
                Margin="0, 18, 0, 0"
                 Content="Войти"
                 Style="{StaticResource MaterialDesignOutlinedLightButton}"

  />
            <Button FontSize="10" Content="Нет аккаунта? Зарегистрироваться" Style="{StaticResource MaterialDesignFlatButton}"></Button>
        </StackPanel>
    </Grid>
</Window>
